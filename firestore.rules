rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore is accessed only by the trusted FastAPI service through IAM.
    // Firebase Admin SDK requests bypass these client security rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
